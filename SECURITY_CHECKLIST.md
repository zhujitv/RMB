# 安全验收检查清单

## 登录与会话

- [ ] 未登录访问首页只显示登录页。
- [ ] 未登录访问业务 API 返回 401。
- [ ] 登录必须验证 email + password。
- [ ] 邮箱登录大小写不敏感。
- [ ] 密码使用 bcrypt/argon2 校验。
- [ ] 不存在明文密码。
- [ ] 登录失败 5 次锁定 30 分钟。
- [ ] 密码至少 8 位且包含字母和数字。
- [ ] `PENDING` 用户提示“账号待管理员审核”。
- [ ] `DISABLED` 用户无法登录。
- [ ] 已停用用户旧 session 立即失效。
- [ ] 首次登录必须修改密码，不能看到业务系统。
- [ ] 退出登录后 session 被撤销。
- [ ] Cookie 为 httpOnly，生产环境 secure，sameSite 合理。

## 权限控制

- [ ] 所有业务 API 调用 `getActor()` 或等效认证中间件。
- [ ] 管理员接口仅管理员可调用。
- [ ] 业务员只能访问自己客户和订单。
- [ ] 财务不能修改系统设置和用户权限。
- [ ] 成本录入员只能维护自己录入或被授权的成本。
- [ ] 物流资料录入员只能访问被授权的物流信息订单。
- [ ] 查看者不能新增、编辑、删除。
- [ ] 篡改 `orderId` 不能访问他人订单。
- [ ] 篡改 `customerId` 不能访问他人客户。
- [ ] 篡改 `supplierId` 不能访问他人供应商。
- [ ] 篡改 `documentId` 不能下载他人附件。

## 财务安全

- [ ] 收款录入和到账确认权限分离。
- [ ] 录入人不能确认自己录入的收款。
- [ ] 成本录入员不能自确认成本。
- [ ] 已确认成本修改必须撤销确认或走审批。
- [ ] 汇率手动修改仅管理员/财务可操作。
- [ ] 汇率修改必须填写原因并记录日志。
- [ ] 提成比例仅管理员/财务可维护。
- [ ] 提成结算前校验订单已收齐、成本已确认、提成比例大于 0。
- [ ] 已结算提成后基础数据修改受限。

## 退税安全

- [ ] 总体完整度不足 100% 时后端禁止提交退税。
- [ ] 管理员强制提交必须开启系统开关。
- [ ] 管理员强制提交必须填写原因。
- [ ] 提交退税后 `taxArchived=true`。
- [ ] 已提交退税订单从当前退税资料列表隐藏。
- [ ] 已归档订单在退税档案可查询、查看、下载。
- [ ] 取消归档仅管理员可操作。
- [ ] 取消归档必须记录操作日志。
- [ ] 归档不是删除，订单、收款、成本、附件均保留。

## 附件上传 / 下载 / 预览

- [ ] 上传仅允许 PDF。
- [ ] 后端校验 MIME 为 `application/pdf`。
- [ ] 后端校验扩展名 `.pdf`。
- [ ] 后端校验文件头 `%PDF-`。
- [ ] 后端校验文件尾 `%%EOF`。
- [ ] 单文件大小限制 20MB 或配置值。
- [ ] 文件名经过清洗。
- [ ] R2 storageKey 随机且不可预测。
- [ ] R2 Bucket 私有，不配置公开 URL。
- [ ] 下载接口必须登录。
- [ ] 下载接口必须校验订单归属和角色权限。
- [ ] 预览接口返回 inline PDF，不返回 JSON。
- [ ] 删除附件为软删除或归档，不物理删除 R2 原件。
- [ ] 上传、下载、预览、删除均写操作日志。

## 报表与导出

- [ ] 报表查询必须鉴权。
- [ ] 报表导出必须鉴权。
- [ ] 普通用户不能导出全量敏感数据。
- [ ] 业务员只能导出自己权限内数据。
- [ ] CSV 导出防护 `= + - @` 公式注入。
- [ ] 导出不包含数据库内部 ID。
- [ ] 大批量导出有行数限制。
- [ ] 导出写入操作日志。
- [ ] 报表支持当前业务、已归档业务、全部业务筛选。

## 数据删除

- [ ] 订单不物理删除。
- [ ] 客户不物理删除。
- [ ] 供应商不物理删除。
- [ ] 收款不物理删除。
- [ ] 成本不物理删除。
- [ ] 退税资料不物理删除。
- [ ] 删除/作废/停用必须填写原因。
- [ ] 删除日志记录操作人、时间、IP、User-Agent、删除前数据、原因。

## 操作日志

- [ ] 登录成功记录。
- [ ] 登录失败记录。
- [ ] 退出登录记录。
- [ ] 新建/修改/删除订单记录。
- [ ] 收款新增/修改/确认记录。
- [ ] 成本新增/修改/确认记录。
- [ ] 汇率修改记录。
- [ ] 提成比例修改记录。
- [ ] 提交退税记录。
- [ ] 取消归档记录。
- [ ] 下载资料包记录。
- [ ] 导出报表记录。
- [ ] 用户权限修改记录。
- [ ] 日志包含操作人、时间、IP、User-Agent、对象、修改前、修改后、结果。
- [ ] 日志不记录密码、token、Access Key、Secret Key。

## 数据库

- [ ] `User.email` 有大小写不敏感唯一保障。
- [ ] `ReceivableOrder.orderNo` 有数据库唯一约束。
- [ ] 客户名称有规范化唯一保障。
- [ ] 供应商名称有规范化唯一保障。
- [ ] 主业务表有 `createdAt/updatedAt`。
- [ ] 主业务表有 `createdBy/updatedBy`。
- [ ] 主业务表有 `deletedAt` 或状态字段。
- [ ] 退税归档字段完整。
- [ ] 附件表保存 R2 key，不保存公开 URL。
- [ ] 敏感配置不进入数据库明文日志。

## API 安全

- [ ] 所有 API 有统一错误返回格式。
- [ ] 生产环境不返回堆栈。
- [ ] 所有 POST/PATCH/DELETE 校验同源或 CSRF Token。
- [ ] 所有请求参数使用 Zod/Joi/Yup 校验。
- [ ] 无 SQL 注入风险。
- [ ] 无 SSRF 风险。
- [ ] 无宽 CORS。
- [ ] 定时任务接口校验 `CRON_SECRET`。
- [ ] 上传接口限制频率。
- [ ] 登录接口限制频率。

## 前端安全

- [ ] 未登录不渲染业务菜单。
- [ ] 未登录不请求业务数据。
- [ ] 首次改密不渲染后台。
- [ ] 前端不暴露 R2 Key。
- [ ] 前端不暴露数据库内部 ID。
- [ ] 不使用危险 `innerHTML` 渲染用户输入。
- [ ] 表单前端校验和后端校验一致。
- [ ] 按钮 loading 防止重复提交。

## 部署安全

- [ ] `.env` 未提交到 Git。
- [ ] `DATABASE_URL` 未硬编码。
- [ ] `JWT_SECRET` 或 session secret 足够强。
- [ ] R2 Access Key 仅服务端环境变量。
- [ ] Vercel build 不自动执行生产迁移。
- [ ] 迁移通过 `npm run db:deploy` 独立执行。
- [ ] 安全响应头已配置。
- [ ] 强制 HTTPS。
- [ ] 生产环境关闭 debug 详细错误。
- [ ] PostgreSQL 有备份策略。
- [ ] R2 有版本保留或生命周期策略。
